# Configuration Apache pour la Revue de Théologie UPC
# Version simplifiée pour éviter les erreurs 500

# Activer le moteur de réécriture (optionnel - désactivé pour debug)
# <IfModule mod_rewrite.c>
#     RewriteEngine On
#     
#     # Permettre l'accès direct aux fichiers et dossiers
#     RewriteCond %{REQUEST_FILENAME} -f [OR]
#     RewriteCond %{REQUEST_FILENAME} -d
#     RewriteRule ^ - [L]
# </IfModule>

# Encodage UTF-8
AddDefaultCharset UTF-8

# Note: Les directives PHP (php_value) peuvent causer des erreurs 500
# si elles ne sont pas supportées. Elles sont commentées.
# Configurez-les plutôt dans php.ini ou .user.ini
# php_value upload_max_filesize 10M
# php_value post_max_size 10M
# php_value max_execution_time 300
# php_value max_input_time 300

